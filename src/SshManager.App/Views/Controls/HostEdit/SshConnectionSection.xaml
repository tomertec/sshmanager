<UserControl x:Class="SshManager.App.Views.Controls.HostEdit.SshConnectionSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="HostEditResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <!-- Hostname -->
        <TextBlock Text="Hostname *"
                   Style="{StaticResource SectionLabelStyle}" />
        <ui:TextBox Text="{Binding Hostname, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="192.168.1.100 or server.example.com"
                    Margin="0,0,0,16" />

        <!-- Port -->
        <TextBlock Text="Port"
                   Style="{StaticResource SectionLabelStyle}" />
        <ui:NumberBox Value="{Binding Port}"
                      Minimum="1"
                      Maximum="65535"
                      SpinButtonPlacementMode="Compact"
                      Margin="0,0,0,16" />

        <!-- Username -->
        <TextBlock Text="Username *"
                   Style="{StaticResource SectionLabelStyle}" />
        <ui:TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="root"
                    Margin="0,0,0,16" />

        <!-- Host Profile -->
        <TextBlock Text="Host Profile"
                   Style="{StaticResource SectionLabelStyle}" />
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0"
                      ItemsSource="{Binding AvailableHostProfiles}"
                      SelectedItem="{Binding SelectedHostProfile}"
                      Margin="0,0,4,0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Document24" FontSize="14" Margin="0,0,6,0" />
                            <TextBlock Text="{Binding DisplayName}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ui:Button Grid.Column="1"
                       Icon="{ui:SymbolIcon Dismiss16}"
                       ToolTip="Clear selection"
                       Command="{Binding ClearHostProfileCommand}"
                       Width="32" Height="32"
                       Padding="0"
                       Margin="0,0,4,0" />
            <ui:Button Grid.Column="2"
                       x:Name="ManageHostProfilesButton"
                       Icon="{ui:SymbolIcon Settings16}"
                       ToolTip="Manage host profiles"
                       Click="ManageHostProfiles_Click"
                       Width="32" Height="32"
                       Padding="0" />
        </Grid>
        <TextBlock Text="Optional profile with common settings to apply to this host"
                   Style="{StaticResource HintTextStyle}"
                   Margin="0,0,0,16" />
    </StackPanel>
</UserControl>
