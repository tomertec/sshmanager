<UserControl x:Class="SshManager.App.Views.Controls.RecordingStatusOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Border CornerRadius="4" Padding="8,4" Background="#CC000000"
            HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8"
            Visibility="{Binding IsRecording, Converter={StaticResource BooleanToVisibilityConverter}}">
        <StackPanel Orientation="Horizontal">
            <Ellipse x:Name="RecordingDot" Width="8" Height="8" Fill="Red">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                               From="1" To="0.3" Duration="0:0:0.5" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
            <TextBlock Text="Recording" Margin="6,0,0,0" Foreground="White" FontSize="12"/>
            <TextBlock Text="{Binding RecordingDuration, StringFormat=hh\\:mm\\:ss}"
                       Margin="8,0,0,0" Foreground="#CCC" FontSize="12"/>
        </StackPanel>
    </Border>
</UserControl>
