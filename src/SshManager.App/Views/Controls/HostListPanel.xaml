<UserControl x:Class="SshManager.App.Views.Controls.HostListPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:converters="clr-namespace:SshManager.App.Converters"
             xmlns:behaviors="clr-namespace:SshManager.App.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="280"
             Background="{ui:ThemeResource ApplicationBackgroundBrush}">

    <UserControl.Resources>
        <!-- Binding proxy for context menus (which exist outside the visual tree) -->
        <converters:BindingProxy x:Key="ViewModelProxy" Data="{Binding}" />

        <!-- Easing functions for animations -->
        <CubicEase x:Key="EaseOut" EasingMode="EaseOut" />

        <!-- Pulsing Status Dot Style -->
        <Style x:Key="PulsingStatusDot" TargetType="Ellipse">
            <Style.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource HostHasActiveSessionConverter}">
                            <Binding Path="Id" />
                            <Binding Path="DataContext.Session.Sessions" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="1" To="0.4" Duration="0:0:1"
                                                 AutoReverse="True"
                                                 EasingFunction="{StaticResource EaseOut}"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Skeleton Loading Template for Host List -->
        <DataTemplate x:Key="HostSkeletonTemplate">
            <Border Background="#08FFFFFF" CornerRadius="8" Padding="12" Margin="4,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="#15FFFFFF" Margin="0,0,10,0" VerticalAlignment="Center" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <Border Width="120" Height="14" Background="#15FFFFFF" CornerRadius="4">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                     From="0.3" To="0.6" Duration="0:0:0.8"
                                                                     AutoReverse="True" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <Border Width="80" Height="10" Background="#10FFFFFF" CornerRadius="4" Margin="0,6,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                     From="0.3" To="0.6" Duration="0:0:0.8"
                                                                     AutoReverse="True">
                                                        <DoubleAnimation.BeginTime>0:0:0.1</DoubleAnimation.BeginTime>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search Box with Settings Button -->
        <Grid Grid.Row="0" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox x:Name="SearchBox"
                       Grid.Column="0"
                       PlaceholderText="Search hosts... (Ctrl+F)"
                       Text="{Binding HostManagement.SearchText, UpdateSourceTrigger=PropertyChanged}"
                       Icon="{ui:SymbolIcon Search24}" />
            <ui:Button Grid.Column="1"
                      Icon="{ui:SymbolIcon Settings24}"
                      ToolTip="Settings (Ctrl+,)"
                      Click="SettingsButton_Click"
                      Padding="8,6"
                      Margin="4,0,0,0" />
        </Grid>

        <!-- Tag Filter Section -->
        <Border Grid.Row="1"
                Margin="8,0,8,8"
                Background="#0C000000"
                BorderBrush="#1AFFFFFF"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8,6"
                Visibility="{Binding HostManagement.AllTags.Count, Converter={StaticResource CountToVisibilityConverter}, FallbackValue=Collapsed}">
            <StackPanel>
                <!-- Selected Filter Tags (with remove buttons) -->
                <ItemsControl ItemsSource="{Binding HostManagement.SelectedFilterTags}"
                             Visibility="{Binding HostManagement.SelectedFilterTags.Count, Converter={StaticResource CountToVisibilityConverter}, FallbackValue=Collapsed}"
                             Margin="0,0,0,4">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Selected tag chip with X button -->
                            <Border Background="#3F0078D4"
                                   BorderBrush="#0078D4"
                                   BorderThickness="1"
                                   CornerRadius="10"
                                   Padding="8,3"
                                   Margin="0,0,4,4">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Tag16"
                                                  FontSize="10"
                                                  Margin="0,0,4,0"
                                                  VerticalAlignment="Center"
                                                  Foreground="#0078D4" />
                                    <TextBlock Text="{Binding Name}"
                                              FontSize="11"
                                              VerticalAlignment="Center"
                                              Foreground="#FFFFFF" />
                                    <ui:Button Icon="{ui:SymbolIcon Dismiss12}"
                                              Command="{Binding DataContext.HostManagement.ToggleTagFilterCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=3}}"
                                              CommandParameter="{Binding}"
                                              Width="16" Height="16"
                                              Padding="0"
                                              Margin="4,0,0,0"
                                              Background="Transparent"
                                              ToolTip="Remove tag filter" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Available Tags (clickable chips) -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                              Text="Tags:"
                              FontSize="11"
                              Opacity="0.6"
                              VerticalAlignment="Center"
                              Margin="0,0,6,0" />

                    <ItemsControl Grid.Column="1"
                                 ItemsSource="{Binding HostManagement.AllTags}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <!-- Tag chip -->
                                <Border x:Name="TagChip"
                                       Background="#1AFFFFFF"
                                       BorderBrush="#2AFFFFFF"
                                       BorderThickness="1"
                                       CornerRadius="10"
                                       Padding="8,3"
                                       Margin="0,0,4,4"
                                       Cursor="Hand"
                                       ToolTip="Click to filter by this tag">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                    Command="{Binding DataContext.HostManagement.ToggleTagFilterCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=4}}"
                                                    CommandParameter="{Binding}" />
                                    </Border.InputBindings>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <!-- Highlight when hovered -->
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#2AFFFFFF" />
                                                    <Setter Property="BorderBrush" Value="#3FFFFFFF" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="Tag16"
                                                      FontSize="10"
                                                      Margin="0,0,4,0"
                                                      VerticalAlignment="Center"
                                                      Opacity="0.7" />
                                        <TextBlock Text="{Binding Name}"
                                                  FontSize="11"
                                                  VerticalAlignment="Center"
                                                  Opacity="0.8" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Groups Section - Compact Filter Bar -->
        <Grid Grid.Row="2" Margin="8,4,8,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Group Filter Dropdown with Management Options -->
            <ui:DropDownButton x:Name="GroupFilterDropdown"
                              Grid.Column="0"
                              MinWidth="90"
                              MaxWidth="140"
                              Padding="6,6,8,6"
                              HorizontalContentAlignment="Left"
                              VerticalAlignment="Center">
                <ui:DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Margin="-2,0,0,0">
                        <!-- Color indicator circle (for selected group with colors) -->
                        <Ellipse Width="10" Height="10"
                                Margin="0,0,5,0"
                                VerticalAlignment="Center"
                                Visibility="{Binding HostManagement.SelectedGroupFilter.Color, Converter={StaticResource GroupColorToVisibilityConverter}}">
                            <Ellipse.Fill>
                                <Binding Path="HostManagement.SelectedGroupFilter.Color" Converter="{StaticResource GroupColorConverter}" />
                            </Ellipse.Fill>
                        </Ellipse>
                        <ui:SymbolIcon Symbol="Grid24" Margin="0,0,6,0" FontSize="14" />
                        <TextBlock x:Name="GroupFilterText" Text="All Groups" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:DropDownButton.Content>
                <ui:DropDownButton.Flyout>
                    <ContextMenu x:Name="GroupFilterMenu" MinWidth="200">
                        <!-- Group Management Section -->
                        <ui:MenuItem Header="Add New Group"
                                    Icon="{ui:SymbolIcon Add16}"
                                    Command="{Binding HostManagement.AddGroupCommand}" />
                        <ui:MenuItem Header="Edit Selected Group"
                                    Icon="{ui:SymbolIcon Edit16}"
                                    Command="{Binding HostManagement.EditGroupCommand}"
                                    CommandParameter="{Binding HostManagement.SelectedGroupFilter}"
                                    IsEnabled="{Binding HostManagement.SelectedGroupFilter, Converter={StaticResource NullToBoolConverter}}" />
                        <ui:MenuItem Header="Delete Selected Group"
                                    Icon="{ui:SymbolIcon Delete16}"
                                    Command="{Binding HostManagement.DeleteGroupCommand}"
                                    CommandParameter="{Binding HostManagement.SelectedGroupFilter}"
                                    IsEnabled="{Binding HostManagement.SelectedGroupFilter, Converter={StaticResource NullToBoolConverter}}" />
                        <Separator />
                        <!-- Groups list will be populated in code-behind -->
                    </ContextMenu>
                </ui:DropDownButton.Flyout>
            </ui:DropDownButton>

            <!-- Action Buttons: Quick Connect, Add Host, Help -->
            <StackPanel Grid.Column="2"
                       Orientation="Horizontal"
                       VerticalAlignment="Center">
                <!-- Quick Connect Overlay Button (Ctrl+K) -->
                <ui:Button Icon="{ui:SymbolIcon Search24}"
                          ToolTip="Quick Connect (Ctrl+K)"
                          Click="QuickConnectOverlayButton_Click"
                          Padding="8,6"
                          Margin="0,0,4,0" />
                <!-- Add Host Button -->
                <ui:Button Icon="{ui:SymbolIcon Add24}"
                          ToolTip="Add Host (Ctrl+N)"
                          Command="{Binding HostManagement.AddHostCommand}"
                          Padding="8,6"
                          Margin="0,0,4,0" />
                <!-- Help Button -->
                <ui:DropDownButton Icon="{ui:SymbolIcon QuestionCircle24}"
                                  ToolTip="Help"
                                  Padding="8,6">
                    <ui:DropDownButton.Flyout>
                        <ContextMenu>
                            <ui:MenuItem Header="Keyboard Shortcuts"
                                        Icon="{ui:SymbolIcon Keyboard24}"
                                        Click="KeyboardShortcutsMenuItem_Click" />
                            <Separator />
                            <ui:MenuItem Header="About SSH Manager"
                                        Icon="{ui:SymbolIcon Info24}"
                                        Click="AboutMenuItem_Click" />
                        </ContextMenu>
                    </ui:DropDownButton.Flyout>
                </ui:DropDownButton>
            </StackPanel>
        </Grid>

        <!-- Host List (with skeleton loading and empty state) -->
        <Grid Grid.Row="3">
            <!-- Skeleton Loading State -->
            <ItemsControl Visibility="{Binding IsLoadingHosts, Converter={StaticResource BoolToVisibilityConverter}}"
                         Margin="8,8,8,0">
                <ItemsControl.ItemTemplate>
                    <StaticResource ResourceKey="HostSkeletonTemplate" />
                </ItemsControl.ItemTemplate>
                <ItemsControl.Items>
                    <system:Int32>1</system:Int32>
                    <system:Int32>2</system:Int32>
                    <system:Int32>3</system:Int32>
                    <system:Int32>4</system:Int32>
                    <system:Int32>5</system:Int32>
                </ItemsControl.Items>
            </ItemsControl>

            <!-- Empty State (when no hosts and not loading) -->
            <StackPanel Visibility="{Binding HostManagement.HasNoHosts, Converter={StaticResource BoolToVisibilityConverter}}"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        Margin="20">
                <ui:SymbolIcon Symbol="DesktopSignal24" FontSize="48" Opacity="0.2"/>
                <TextBlock Text="No hosts yet"
                           FontSize="18" FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Opacity="0.7" Margin="0,16,0,4"/>
                <TextBlock Text="Add your first SSH connection to get started"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Opacity="0.4" TextWrapping="Wrap" TextAlignment="Center"
                           MaxWidth="200"/>
                <Button Command="{Binding HostManagement.AddHostCommand}"
                        Padding="16,10"
                        FontSize="14"
                        Margin="0,20,0,0"
                        HorizontalAlignment="Center"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="ButtonBorder"
                                                Background="#286fe0"
                                                CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <TextBlock Text="Add Host" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="#3a7de8"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1e5bb8"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <!-- Actual Host List -->
            <ListBox x:Name="HostListBox"
                     ItemsSource="{Binding HostManagement.Hosts}"
                     SelectedItem="{Binding HostManagement.SelectedHost}"
                     Margin="8,8,8,0"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     VirtualizingPanel.ScrollUnit="Pixel"
                     VirtualizingPanel.CacheLength="20,20"
                     VirtualizingPanel.CacheLengthUnit="Item"
                     behaviors:ListBoxDragDropBehavior.IsEnabled="True"
                     behaviors:ListBoxDragDropBehavior.ReorderCommand="{Binding HostManagement.ReorderHostsCommand}"
                     Visibility="{Binding HostManagement.HasNoHosts, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- Outer wrapper: carries context menu and active-session accent border -->
                    <Border x:Name="OuterBorder"
                            CornerRadius="{StaticResource CardCornerRadius}"
                            BorderThickness="2"
                            Margin="{StaticResource CardMargin}"
                            Padding="0">
                        <Border.ContextMenu>
                            <ContextMenu>
                                <ui:MenuItem Header="Connect"
                                            Icon="{ui:SymbolIcon Play16}"
                                            Command="{Binding Data.Session.ConnectCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}"
                                            FontWeight="SemiBold"
                                            IsEnabled="{Binding Data.Session.IsConnecting, Source={StaticResource ViewModelProxy}, Converter={StaticResource InverseBoolConverter}}" />
                                <ui:MenuItem Command="{Binding Data.HostManagement.ToggleFavoriteCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}">
                                    <ui:MenuItem.Style>
                                        <Style TargetType="ui:MenuItem">
                                            <Setter Property="Header" Value="Add to Favorites" />
                                            <Setter Property="Icon" Value="{ui:SymbolIcon Star16}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                    <Setter Property="Header" Value="Remove from Favorites" />
                                                    <Setter Property="Icon" Value="{ui:SymbolIcon StarOff16}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:MenuItem.Style>
                                </ui:MenuItem>
                                <Separator />
                                <ui:MenuItem Header="Copy Hostname"
                                            Icon="{ui:SymbolIcon Copy16}"
                                            Command="{Binding Data.HostManagement.CopyHostnameCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}" />
                                <ui:MenuItem Header="Copy IP Address"
                                            Icon="{ui:SymbolIcon Globe16}"
                                            Command="{Binding Data.HostManagement.CopyIpAddressCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}" />
                                <Separator />
                                <ui:MenuItem Header="Duplicate Host"
                                            Icon="{ui:SymbolIcon DocumentCopy16}"
                                            Command="{Binding Data.HostManagement.DuplicateHostCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}" />
                                <ui:MenuItem Header="Edit"
                                            Icon="{ui:SymbolIcon Edit16}"
                                            Command="{Binding Data.HostManagement.EditHostCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}" />
                                <ui:MenuItem Header="SFTP Browser"
                                            Icon="{ui:SymbolIcon FolderOpen16}"
                                            Command="{Binding Data.SftpLauncher.OpenSftpBrowserForHostCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}" />
                                <Separator />
                                <ui:MenuItem Header="Delete"
                                            Icon="{ui:SymbolIcon Delete16}"
                                            Command="{Binding Data.HostManagement.DeleteHostCommand, Source={StaticResource ViewModelProxy}}"
                                            CommandParameter="{Binding}" />
                            </ContextMenu>
                        </Border.ContextMenu>
                        <!-- Active session accent border color -->
                        <Border.BorderBrush>
                            <MultiBinding Converter="{StaticResource HostActiveSessionBorderConverter}">
                                <Binding Path="Id" />
                                <Binding Path="DataContext.Session.Sessions" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                            </MultiBinding>
                        </Border.BorderBrush>

                        <!-- Clip grid to outer rounded corner so the color stripe respects the radius -->
                        <Grid ClipToBounds="True">
                            <!-- Group color accent bar (left edge, 4px wide, rounded on the visible side) -->
                            <Border x:Name="GroupAccentBar"
                                    Width="4"
                                    HorizontalAlignment="Left"
                                    CornerRadius="2,0,0,2"
                                    Visibility="{Binding Group.Color, Converter={StaticResource GroupColorToVisibilityConverter}}">
                                <Border.Background>
                                    <Binding Path="Group.Color" Converter="{StaticResource GroupColorConverter}" />
                                </Border.Background>
                            </Border>

                            <!-- Card surface with hover animation -->
                            <Border x:Name="CardBorder"
                                    CornerRadius="{StaticResource CardCornerRadius}"
                                    Background="{DynamicResource SurfaceCardBrush}"
                                    BorderBrush="{DynamicResource StrokeSurfaceBrush}"
                                    BorderThickness="1"
                                    Padding="10,8"
                                    Margin="0"
                                    Cursor="Hand"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform ScaleX="1" ScaleY="1" />
                                        <TranslateTransform X="0" Y="0" />
                                    </TransformGroup>
                                </Border.RenderTransform>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <!-- Fade-slide in on load -->
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                        From="0" To="1" Duration="0:0:0.25">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CubicEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)"
                                                                        From="12" To="0" Duration="0:0:0.25">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CubicEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                            <!-- Hover: subtle scale + surface color lift -->
                                            <EventTrigger RoutedEvent="MouseEnter">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                                                        To="1.015" Duration="0:0:0.12">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                                                        To="1.015" Duration="0:0:0.12">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                            <EventTrigger RoutedEvent="MouseLeave">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                                                        To="1" Duration="0:0:0.18">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                                                        To="1" Duration="0:0:0.18">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                            <!-- Hover background via DataTrigger on IsMouseOver -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SurfaceCardHoverBrush}" />
                                                <Setter Property="BorderBrush" Value="{DynamicResource StrokeDefaultBrush}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <!-- Card content grid: 3 rows -->
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!-- ROW 0: Status dot + Display name + right-side icons -->
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Status indicator dot (12x12) with glow -->
                                        <Grid Grid.Column="0" Width="12" Height="12"
                                              Margin="0,0,9,0"
                                              VerticalAlignment="Center">
                                            <!-- Glow halo: same color, blurred, larger -->
                                            <Ellipse x:Name="StatusGlow"
                                                     Width="12" Height="12"
                                                     Opacity="0.35">
                                                <Ellipse.Effect>
                                                    <BlurEffect Radius="5" />
                                                </Ellipse.Effect>
                                                <Ellipse.Fill>
                                                    <MultiBinding Converter="{StaticResource HostStatusToColorConverter}">
                                                        <Binding Path="Id" />
                                                        <Binding Path="DataContext.HostStatuses" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                                    </MultiBinding>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <!-- Solid dot -->
                                            <Ellipse Width="8" Height="8"
                                                     Style="{StaticResource PulsingStatusDot}">
                                                <Ellipse.ToolTip>
                                                    <MultiBinding Converter="{StaticResource HostStatusToTooltipConverter}">
                                                        <Binding Path="Id" />
                                                        <Binding Path="DataContext.HostStatuses" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                                    </MultiBinding>
                                                </Ellipse.ToolTip>
                                                <Ellipse.Fill>
                                                    <MultiBinding Converter="{StaticResource HostStatusToColorConverter}">
                                                        <Binding Path="Id" />
                                                        <Binding Path="DataContext.HostStatuses" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                                    </MultiBinding>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                        </Grid>

                                        <!-- Display name (Line 1, primary) -->
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding DisplayName}"
                                                   FontWeight="SemiBold"
                                                   FontSize="13"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"
                                                   Foreground="{DynamicResource TextPrimaryBrush}" />

                                        <!-- Right-side icons: connection type, favorite, action buttons -->
                                        <StackPanel Grid.Column="2"
                                                    Orientation="Horizontal"
                                                    VerticalAlignment="Center"
                                                    Margin="6,0,0,0">

                                            <!-- Connection type icon -->
                                            <ui:SymbolIcon Symbol="{Binding ConnectionType, Converter={StaticResource ConnectionTypeIconConverter}}"
                                                           FontSize="12"
                                                           Margin="0,0,4,0"
                                                           VerticalAlignment="Center"
                                                           Foreground="{DynamicResource TextSecondaryBrush}">
                                                <ui:SymbolIcon.ToolTip>
                                                    <TextBlock>
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="SSH Connection" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ConnectionType}" Value="Serial">
                                                                        <Setter Property="Text" Value="Serial Connection" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </ui:SymbolIcon.ToolTip>
                                            </ui:SymbolIcon>

                                            <!-- Favorite star -->
                                            <ui:Button Command="{Binding DataContext.HostManagement.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                       CommandParameter="{Binding}"
                                                       Width="22" Height="22"
                                                       Padding="0"
                                                       CornerRadius="4"
                                                       Margin="0,0,2,0">
                                                <ui:Button.Style>
                                                    <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                                        <Setter Property="Background" Value="Transparent" />
                                                        <Setter Property="BorderThickness" Value="0" />
                                                        <Setter Property="ToolTip" Value="Add to Favorites" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                                <Setter Property="ToolTip" Value="Remove from Favorites" />
                                                            </DataTrigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource SurfaceSubtleHoverBrush}" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ui:Button.Style>
                                                <ui:SymbolIcon FontSize="13">
                                                    <ui:SymbolIcon.Style>
                                                        <Style TargetType="ui:SymbolIcon">
                                                            <Setter Property="Symbol" Value="StarOff16" />
                                                            <Setter Property="Foreground" Value="{DynamicResource TextTertiaryBrush}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                                    <Setter Property="Symbol" Value="Star16" />
                                                                    <Setter Property="Foreground" Value="#FFD700" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ui:SymbolIcon.Style>
                                                </ui:SymbolIcon>
                                            </ui:Button>

                                            <!-- SFTP button -->
                                            <ui:Button Width="22" Height="22"
                                                       Padding="0"
                                                       CornerRadius="4"
                                                       Command="{Binding DataContext.SftpLauncher.OpenSftpBrowserForHostCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                       CommandParameter="{Binding}"
                                                       ToolTip="SFTP Browser"
                                                       Margin="0,0,2,0">
                                                <ui:Button.Style>
                                                    <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                                        <Setter Property="Background" Value="{DynamicResource AccentPrimaryBrush}" />
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource AccentSecondaryBrush}" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ui:Button.Style>
                                                <ui:SymbolIcon Symbol="FolderOpen16" FontSize="11" Foreground="{DynamicResource TextOnAccentBrush}" />
                                            </ui:Button>

                                            <!-- Edit button -->
                                            <ui:Button Icon="{ui:SymbolIcon Edit16}"
                                                       Command="{Binding DataContext.HostManagement.EditHostCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                       CommandParameter="{Binding}"
                                                       ToolTip="Edit (Ctrl+E)"
                                                       Width="22" Height="22"
                                                       Padding="0"
                                                       Margin="0,0,2,0" />

                                            <!-- Connect button -->
                                            <ui:Button Command="{Binding DataContext.Session.ConnectCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                       CommandParameter="{Binding}"
                                                       ToolTip="Connect (Enter)"
                                                       Width="22" Height="22"
                                                       Padding="0"
                                                       CornerRadius="4"
                                                       IsEnabled="{Binding DataContext.Session.IsConnecting, RelativeSource={RelativeSource AncestorType=ListBox}, Converter={StaticResource InverseBoolConverter}}">
                                                <ui:Button.Style>
                                                    <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                                        <Setter Property="Background" Value="{DynamicResource AccentPrimaryBrush}" />
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource AccentSecondaryBrush}" />
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource AccentTertiaryBrush}" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ui:Button.Style>
                                                <ui:SymbolIcon Symbol="Play16" FontSize="11" Foreground="{DynamicResource TextOnAccentBrush}" />
                                            </ui:Button>
                                        </StackPanel>
                                    </Grid>

                                    <!-- ROW 1: username@hostname secondary line -->
                                    <Grid Grid.Row="1" Margin="21,3,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <!-- Username prefix (only shown when non-empty) -->
                                            <TextBlock Text="{Binding Username}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       TextTrimming="CharacterEllipsis"
                                                       Visibility="{Binding Username, Converter={StaticResource StringToVisibilityConverter}}" />
                                            <!-- @ separator (only shown when username is set) -->
                                            <TextBlock Text="@"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       Visibility="{Binding Username, Converter={StaticResource StringToVisibilityConverter}}" />
                                            <!-- Hostname -->
                                            <TextBlock Text="{Binding Hostname}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </Grid>

                                    <!-- ROW 2: Tag chips (only when the host has tags) -->
                                    <ItemsControl Grid.Row="2"
                                                  ItemsSource="{Binding Tags}"
                                                  Margin="21,5,0,0"
                                                  Visibility="{Binding Tags.Count, Converter={StaticResource CountToVisibilityConverter}, FallbackValue=Collapsed}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="{StaticResource PillCornerRadius}"
                                                        Padding="6,2"
                                                        Margin="0,0,4,2"
                                                        Background="{DynamicResource SurfaceSubtleBrush}"
                                                        BorderBrush="{DynamicResource StrokeSecondaryBrush}"
                                                        BorderThickness="1">
                                                    <TextBlock Text="{Binding Name}"
                                                               FontSize="10"
                                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                                               VerticalAlignment="Center" />
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        </Grid>

        <!-- Action Buttons -->
        <Grid Grid.Row="4" Margin="8,8,8,12">
            <!-- Left: History, Snippets, Key Manager, and other utility buttons - spread evenly -->
            <UniformGrid Rows="1" HorizontalAlignment="Stretch">
                <ui:Button Icon="{ui:SymbolIcon History24}"
                          ToolTip="Connection History (Ctrl+H)"
                          Click="HistoryButton_Click"
                          HorizontalAlignment="Stretch"
                          Margin="2" />
                <ui:Button Icon="{ui:SymbolIcon Code24}"
                          ToolTip="Snippets (Ctrl+Shift+S)"
                          Click="SnippetsButton_Click"
                          HorizontalAlignment="Stretch"
                          Margin="2" />
                <ui:Button Icon="{ui:SymbolIcon Key24}"
                          ToolTip="SSH Key Manager"
                          Click="KeyManagerButton_Click"
                          HorizontalAlignment="Stretch"
                          Margin="2" />
                <ui:Button Icon="{ui:SymbolIcon Record24}"
                          ToolTip="Session Recordings"
                          Click="RecordingsButton_Click"
                          HorizontalAlignment="Stretch"
                          Margin="2" />
                <ui:Button Icon="{ui:SymbolIcon PlugConnected24}"
                          ToolTip="Serial Port Quick Connect (Ctrl+Shift+P)"
                          Click="SerialQuickConnectButton_Click"
                          HorizontalAlignment="Stretch"
                          Margin="2" />
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>
