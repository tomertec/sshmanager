<ui:FluentWindow x:Class="SshManager.App.Views.Dialogs.HostEditDialog"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                xmlns:converters="clr-namespace:SshManager.App.Converters"
                xmlns:hostEdit="clr-namespace:SshManager.App.Views.Controls.HostEdit"
                mc:Ignorable="d"
                Title="{Binding Title}"
                Height="650" Width="480"
                MinHeight="550" MinWidth="420"
                WindowStartupLocation="CenterOwner"
                ResizeMode="CanResizeWithGrip"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title}"
                     ShowMaximize="False"
                     ShowMinimize="False" />

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="24,16">
            <StackPanel>
                <!-- Display Name -->
                <TextBlock Text="Display Name"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                           PlaceholderText="My Server"
                           Margin="0,0,0,16" />

                <!-- Connection Type -->
                <TextBlock Text="Connection Type"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <RadioButton Content="SSH"
                                IsChecked="{Binding IsSshConnection, Mode=TwoWay}"
                                Margin="0,0,16,0" />
                    <RadioButton Content="Serial Port"
                                IsChecked="{Binding IsSerialConnection, Mode=TwoWay}" />
                </StackPanel>

                <!-- SSH Connection Settings -->
                <StackPanel Visibility="{Binding IsSshConnection, Converter={StaticResource BoolToVisibilityConverter}}">
                    <!-- SSH Connection Section (Hostname, Port, Username, Host Profile) -->
                    <hostEdit:SshConnectionSection x:Name="SshConnectionSection" />

                    <!-- Authentication Section (Auth type + all auth methods) -->
                    <hostEdit:AuthenticationSection x:Name="AuthenticationSection" />
                </StackPanel>

                <!-- Serial Port Connection Settings -->
                <hostEdit:SerialConnectionSection 
                    Visibility="{Binding IsSerialConnection, Converter={StaticResource BoolToVisibilityConverter}}" />

                <!-- Group and Tags Section -->
                <hostEdit:GroupAndTagsSection />

                <!-- Notes Section -->
                <hostEdit:NotesSection />

                <!-- Advanced Options Section -->
                <hostEdit:AdvancedOptionsSection x:Name="AdvancedOptionsSection" />
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="1"
                      Content="Cancel"
                      Command="{Binding CancelCommand}"
                      Margin="0,0,8,0"
                      MinWidth="80" />

            <ui:Button Grid.Column="2"
                      Content="Save"
                      Appearance="Primary"
                      Command="{Binding SaveCommand}"
                      MinWidth="80" />
        </Grid>
    </Grid>
</ui:FluentWindow>
