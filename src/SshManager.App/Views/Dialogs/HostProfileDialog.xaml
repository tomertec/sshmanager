<ui:FluentWindow x:Class="SshManager.App.Views.Dialogs.HostProfileDialog"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                xmlns:models="clr-namespace:SshManager.Core.Models;assembly=SshManager.Core"
                xmlns:converters="clr-namespace:SshManager.App.Converters"
                mc:Ignorable="d"
                Title="{Binding Title}"
                Height="600" Width="480"
                MinHeight="550" MinWidth="420"
                WindowStartupLocation="CenterOwner"
                ResizeMode="CanResizeWithGrip"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title}"
                     ShowMaximize="False"
                     ShowMinimize="False" />

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="24,16">
            <StackPanel>
                <!-- Display Name -->
                <TextBlock Text="Display Name *"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                           PlaceholderText="Production Servers Profile"
                           Margin="0,0,0,16" />

                <!-- Description -->
                <TextBlock Text="Description"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                           PlaceholderText="Optional description of this profile..."
                           AcceptsReturn="True"
                           TextWrapping="Wrap"
                           MinHeight="60"
                           MaxHeight="100"
                           Margin="0,0,0,16" />

                <!-- Default Port -->
                <TextBlock Text="Default Port"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ui:NumberBox Value="{Binding DefaultPort}"
                             Minimum="1"
                             Maximum="65535"
                             SpinButtonPlacementMode="Compact"
                             Margin="0,0,0,16" />

                <!-- Default Username -->
                <TextBlock Text="Default Username"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding DefaultUsername, UpdateSourceTrigger=PropertyChanged}"
                           PlaceholderText="root (optional)"
                           Margin="0,0,0,16" />

                <!-- Authentication Type -->
                <TextBlock Text="Authentication Type"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ComboBox ItemsSource="{Binding AuthTypes}"
                         SelectedItem="{Binding AuthType}"
                         Margin="0,0,0,16">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding}" Value="{x:Static models:AuthType.SshAgent}">
                                                <Setter Property="Text" Value="SSH Agent (Auto)" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding}" Value="{x:Static models:AuthType.PrivateKeyFile}">
                                                <Setter Property="Text" Value="Private Key File" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding}" Value="{x:Static models:AuthType.Password}">
                                                <Setter Property="Text" Value="Password" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Private Key Path (shown only for PrivateKeyFile auth) -->
                <StackPanel Visibility="{Binding ShowPrivateKeyPath, Converter={StaticResource BoolToVisibilityConverter}}"
                           Margin="0,0,0,16">
                    <TextBlock Text="Private Key File"
                              FontWeight="SemiBold"
                              Margin="0,0,0,4" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:TextBox Grid.Column="0"
                                   Text="{Binding PrivateKeyPath, UpdateSourceTrigger=PropertyChanged}"
                                   PlaceholderText="C:\Users\...\.ssh\id_rsa"
                                   Margin="0,0,8,0" />
                        <ui:Button Grid.Column="1"
                                  Content="Browse..."
                                  Command="{Binding BrowsePrivateKeyCommand}" />
                    </Grid>
                </StackPanel>

                <!-- ProxyJump Profile -->
                <TextBlock Text="ProxyJump Profile"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0"
                             ItemsSource="{Binding AvailableProxyJumpProfiles}"
                             SelectedItem="{Binding SelectedProxyJumpProfile}"
                             Margin="0,0,4,0">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="ArrowSync24" FontSize="14" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding DisplayName}" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <ui:Button Grid.Column="1"
                              Icon="{ui:SymbolIcon Dismiss16}"
                              ToolTip="Clear selection"
                              Command="{Binding ClearProxyJumpProfileCommand}"
                              Width="32" Height="32"
                              Padding="0" />
                </Grid>
                <TextBlock Text="Optional ProxyJump profile that hosts using this profile will inherit"
                          FontSize="11"
                          Opacity="0.6"
                          Margin="0,0,0,16" />

                <!-- Validation Error -->
                <Border Visibility="{Binding ValidationError, Converter={StaticResource StringToVisibilityConverter}}"
                       Background="#30FF4444"
                       BorderBrush="#FFFF4444"
                       BorderThickness="1"
                       CornerRadius="4"
                       Padding="12"
                       Margin="0,0,0,16">
                    <TextBlock Text="{Binding ValidationError}"
                              Foreground="#FFFF4444"
                              TextWrapping="Wrap" />
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="1"
                      Content="Cancel"
                      Command="{Binding CancelCommand}"
                      Margin="0,0,8,0"
                      MinWidth="80" />

            <ui:Button Grid.Column="2"
                      Content="Save"
                      Appearance="Primary"
                      Command="{Binding SaveCommand}"
                      MinWidth="80" />
        </Grid>
    </Grid>
</ui:FluentWindow>
