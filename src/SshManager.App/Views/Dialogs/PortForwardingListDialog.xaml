<ui:FluentWindow x:Class="SshManager.App.Views.Dialogs.PortForwardingListDialog"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                xmlns:converters="clr-namespace:SshManager.App.Converters"
                xmlns:models="clr-namespace:SshManager.Core.Models;assembly=SshManager.Core"
                mc:Ignorable="d"
                Title="Port Forwarding Profiles"
                Height="500" Width="650"
                MinHeight="400" MinWidth="550"
                WindowStartupLocation="CenterOwner"
                ResizeMode="CanResize"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="Port Forwarding Profiles"
                     ShowMaximize="True"
                     ShowMinimize="False" />

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          Text="Manage port forwarding profiles for your SSH connections"
                          Opacity="0.7"
                          VerticalAlignment="Center" />

                <ui:Button Grid.Column="1"
                          Icon="{ui:SymbolIcon Add24}"
                          Content="Add Profile"
                          Appearance="Primary"
                          Click="AddProfile_Click"
                          Padding="12,8" />
            </Grid>

            <!-- Profiles List -->
            <Border Grid.Row="1"
                    Background="#1FFFFFFF"
                    CornerRadius="8"
                    BorderBrush="#2FFFFFFF"
                    BorderThickness="1">
                <Grid>
                    <!-- Profiles DataGrid -->
                    <DataGrid x:Name="ProfilesGrid"
                             ItemsSource="{Binding Profiles}"
                             SelectedItem="{Binding SelectedProfile}"
                             AutoGenerateColumns="False"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             CanUserReorderColumns="False"
                             IsReadOnly="True"
                             SelectionMode="Single"
                             GridLinesVisibility="Horizontal"
                             HeadersVisibility="Column"
                             Background="Transparent"
                             BorderThickness="0"
                             RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <!-- Name -->
                            <DataGridTemplateColumn Header="Name" Width="*" MinWidth="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="8,4">
                                            <TextBlock Text="{Binding DisplayName}"
                                                      FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Description}"
                                                      Opacity="0.6"
                                                      FontSize="11"
                                                      TextTrimming="CharacterEllipsis"
                                                      Visibility="{Binding Description, Converter={StaticResource NullToVisibilityConverter}}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Type -->
                            <DataGridTemplateColumn Header="Type" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="8,4">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding ForwardingType}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ForwardingType}" Value="{x:Static models:PortForwardingType.LocalForward}">
                                                            <Setter Property="Text" Value="Local (-L)" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ForwardingType}" Value="{x:Static models:PortForwardingType.RemoteForward}">
                                                            <Setter Property="Text" Value="Remote (-R)" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ForwardingType}" Value="{x:Static models:PortForwardingType.DynamicForward}">
                                                            <Setter Property="Text" Value="SOCKS5 (-D)" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Local Endpoint -->
                            <DataGridTemplateColumn Header="Local" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding LocalBindAddress, StringFormat='{}{0}:'}"
                                                  Margin="8,4">
                                            <Run Text="{Binding LocalPort}" />
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Remote Endpoint -->
                            <DataGridTemplateColumn Header="Remote" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="8,4">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text">
                                                        <Setter.Value>
                                                            <MultiBinding StringFormat="{}{0}:{1}">
                                                                <Binding Path="RemoteHost" />
                                                                <Binding Path="RemotePort" />
                                                            </MultiBinding>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ForwardingType}" Value="{x:Static models:PortForwardingType.DynamicForward}">
                                                            <Setter Property="Text" Value="(SOCKS5)" />
                                                            <Setter Property="Opacity" Value="0.5" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Status Indicators -->
                            <DataGridTemplateColumn Header="Options" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="8,4">
                                            <ui:SymbolIcon Symbol="Power24"
                                                          ToolTip="Enabled"
                                                          Foreground="#4CAF50"
                                                          Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVisibilityConverter}}"
                                                          Margin="0,0,8,0" />
                                            <ui:SymbolIcon Symbol="Power24"
                                                          ToolTip="Disabled"
                                                          Foreground="#9E9E9E"
                                                          Visibility="{Binding IsEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                                          Margin="0,0,8,0" />
                                            <ui:SymbolIcon Symbol="ArrowSync24"
                                                          ToolTip="Auto-start"
                                                          Foreground="#2196F3"
                                                          Visibility="{Binding AutoStart, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Actions -->
                            <DataGridTemplateColumn Header="" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="4">
                                            <ui:Button Icon="{ui:SymbolIcon Edit16}"
                                                      ToolTip="Edit"
                                                      Click="EditProfile_Click"
                                                      Width="28" Height="28"
                                                      Padding="0"
                                                      Margin="0,0,4,0" />
                                            <ui:Button Icon="{ui:SymbolIcon Delete16}"
                                                      ToolTip="Delete"
                                                      Click="DeleteProfile_Click"
                                                      Width="28" Height="28"
                                                      Padding="0" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#2078D4" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1078D4" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>

                    <!-- Empty State -->
                    <StackPanel VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Visibility="{Binding HasProfiles, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                        <ui:SymbolIcon Symbol="ArrowRouting24"
                                      FontSize="48"
                                      Opacity="0.3"
                                      HorizontalAlignment="Center"
                                      Margin="0,0,0,16" />
                        <TextBlock Text="No port forwarding profiles"
                                  FontSize="16"
                                  Opacity="0.5"
                                  HorizontalAlignment="Center"
                                  Margin="0,0,0,8" />
                        <TextBlock Text="Click 'Add Profile' to create your first port forwarding rule"
                                  FontSize="12"
                                  Opacity="0.4"
                                  HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.Row="1"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="40" Height="40" />
                <TextBlock Text="Loading..." Margin="0,8,0,0" />
            </StackPanel>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                      Text="{Binding Profiles.Count, StringFormat='{}{0} profile(s)'}"
                      Opacity="0.6"
                      VerticalAlignment="Center" />

            <ui:Button Grid.Column="1"
                      Content="Close"
                      Click="Close_Click"
                      MinWidth="80" />
        </Grid>
    </Grid>
</ui:FluentWindow>
