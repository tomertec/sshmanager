<ui:FluentWindow x:Class="SshManager.App.Views.Dialogs.SessionRecoveryDialog"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                mc:Ignorable="d"
                Title="Session Recovery"
                Height="350" Width="500"
                MinHeight="300" MinWidth="450"
                WindowStartupLocation="CenterOwner"
                ResizeMode="NoResize"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="Session Recovery"
                     ShowMaximize="False"
                     ShowMinimize="False" />

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="24,16">
            <!-- Warning Icon and Message -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <ui:SymbolIcon Symbol="Warning24"
                              FontSize="24"
                              Foreground="{DynamicResource SystemFillColorCautionBrush}"
                              Margin="0,0,12,0" />
                <TextBlock Text="Previous Session Recovery"
                          FontSize="16"
                          FontWeight="SemiBold"
                          VerticalAlignment="Center" />
            </StackPanel>

            <TextBlock TextWrapping="Wrap"
                      Margin="0,0,0,16"
                      Opacity="0.8">
                <Run Text="SshManager detected that it did not close normally. The following sessions were active when the application last ran." />
            </TextBlock>

            <!-- Session List -->
            <TextBlock Text="Sessions to restore:"
                      FontWeight="SemiBold"
                      Margin="0,0,0,8" />
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}"
                   CornerRadius="4"
                   MaxHeight="120">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Sessions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="12,6">
                                    <ui:SymbolIcon Symbol="Desktop20"
                                                  FontSize="16"
                                                  Margin="0,0,8,0"
                                                  Opacity="0.7" />
                                    <TextBlock Text="{Binding Title}"
                                              VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <TextBlock Text="{Binding SessionCountText}"
                      Margin="0,8,0,0"
                      FontSize="12"
                      Opacity="0.6" />
        </StackPanel>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="1"
                      Content="Don't Restore"
                      Command="{Binding DontRestoreCommand}"
                      Margin="0,0,8,0"
                      MinWidth="100" />

            <ui:Button Grid.Column="2"
                      Content="Restore Sessions"
                      Appearance="Primary"
                      Command="{Binding RestoreCommand}"
                      MinWidth="120" />
        </Grid>
    </Grid>
</ui:FluentWindow>
