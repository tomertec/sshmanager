<ui:FluentWindow x:Class="SshManager.App.Views.Dialogs.GroupDialog"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                mc:Ignorable="d"
                Title="{Binding Title}"
                Height="340" Width="400"
                MinHeight="320" MinWidth="350"
                WindowStartupLocation="CenterOwner"
                ResizeMode="NoResize"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title}"
                     ShowMaximize="False"
                     ShowMinimize="False" />

        <!-- Form Content -->
        <StackPanel Grid.Row="1" Margin="24,16">
            <!-- Group Name -->
            <TextBlock Text="Group Name *"
                      FontWeight="SemiBold"
                      Margin="0,0,0,4" />
            <ui:TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                       PlaceholderText="Production Servers"
                       Margin="0,0,0,16" />

            <!-- Icon Selection -->
            <TextBlock Text="Icon"
                      FontWeight="SemiBold"
                      Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding AvailableIcons}"
                     SelectedItem="{Binding Icon}"
                     Margin="0,0,0,16">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="{Binding}" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <!-- Status Check Interval -->
            <TextBlock Text="Status Check Interval (seconds)"
                      FontWeight="SemiBold"
                      Margin="0,0,0,4" />
            <ui:NumberBox Value="{Binding StatusCheckIntervalSeconds}"
                         Minimum="5"
                         Maximum="3600"
                         SpinButtonPlacementMode="Compact"
                         Margin="0,0,0,8" />
            <TextBlock Text="Controls how often hosts in this group are checked."
                      FontSize="11"
                      Opacity="0.6" />
        </StackPanel>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="1"
                      Content="Cancel"
                      Command="{Binding CancelCommand}"
                      Margin="0,0,8,0"
                      MinWidth="80" />

            <ui:Button Grid.Column="2"
                      Content="Save"
                      Appearance="Primary"
                      Command="{Binding SaveCommand}"
                      MinWidth="80" />
        </Grid>
    </Grid>
</ui:FluentWindow>
