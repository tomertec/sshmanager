<ui:FluentWindow x:Class="SshManager.App.Views.Dialogs.ProxyJumpProfileDialog"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                xmlns:converters="clr-namespace:SshManager.App.Converters"
                mc:Ignorable="d"
                Title="{Binding Title}"
                Height="550" Width="500"
                MinHeight="500" MinWidth="450"
                WindowStartupLocation="CenterOwner"
                ResizeMode="NoResize"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title}"
                     ShowMaximize="False"
                     ShowMinimize="False" />

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="24,16">
            <StackPanel>
                <!-- Display Name -->
                <TextBlock Text="Display Name *"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                           PlaceholderText="Production Bastion Chain"
                           Margin="0,0,0,16" />

                <!-- Description -->
                <TextBlock Text="Description"
                          FontWeight="SemiBold"
                          Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                           PlaceholderText="Optional description for this profile..."
                           AcceptsReturn="True"
                           TextWrapping="Wrap"
                           MinHeight="50"
                           MaxHeight="80"
                           Margin="0,0,0,16" />

                <!-- Enabled Toggle -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                              Text="Enabled"
                              FontWeight="SemiBold"
                              VerticalAlignment="Center" />
                    <ToggleButton Grid.Column="1"
                                 IsChecked="{Binding IsEnabled}"
                                 Content="{Binding IsEnabled, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Enabled|Disabled}" />
                </Grid>

                <!-- Jump Chain Section -->
                <TextBlock Text="Jump Chain"
                          FontWeight="SemiBold"
                          FontSize="14"
                          Margin="0,0,0,8" />

                <Border Background="#1FFFFFFF"
                        CornerRadius="8"
                        BorderBrush="#2FFFFFFF"
                        BorderThickness="1"
                        Padding="8"
                        Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" MinHeight="120" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Jump Hosts List -->
                        <ListBox Grid.Row="0"
                                ItemsSource="{Binding JumpHops}"
                                SelectedItem="{Binding SelectedHop}"
                                Background="Transparent"
                                BorderThickness="0"
                                MinHeight="100"
                                MaxHeight="180">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="4" />
                                    <Setter Property="Margin" Value="0,2" />
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1FFFFFFF"
                                           CornerRadius="6"
                                           Padding="10,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Order Number -->
                                            <Border Grid.Column="0"
                                                   Background="#0078D4"
                                                   CornerRadius="12"
                                                   Width="24" Height="24"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,10,0">
                                                <TextBlock Text="{Binding SortOrder, StringFormat='{}{0}'}"
                                                          Foreground="White"
                                                          FontWeight="Bold"
                                                          FontSize="11"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                            </Border>

                                            <!-- Host Info -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding HostDisplayName}"
                                                          FontWeight="SemiBold"
                                                          TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding HostAddress}"
                                                          Opacity="0.6"
                                                          FontSize="11"
                                                          TextTrimming="CharacterEllipsis" />
                                            </StackPanel>

                                            <!-- Action Buttons -->
                                            <StackPanel Grid.Column="2"
                                                       Orientation="Horizontal"
                                                       VerticalAlignment="Center">
                                                <ui:Button Icon="{ui:SymbolIcon ArrowUp16}"
                                                          Command="{Binding DataContext.MoveHopUpCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                          CommandParameter="{Binding}"
                                                          ToolTip="Move Up"
                                                          Width="28" Height="28"
                                                          Padding="0"
                                                          Margin="0,0,4,0" />
                                                <ui:Button Icon="{ui:SymbolIcon ArrowDown16}"
                                                          Command="{Binding DataContext.MoveHopDownCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                          CommandParameter="{Binding}"
                                                          ToolTip="Move Down"
                                                          Width="28" Height="28"
                                                          Padding="0"
                                                          Margin="0,0,4,0" />
                                                <ui:Button Icon="{ui:SymbolIcon Dismiss16}"
                                                          Command="{Binding DataContext.RemoveJumpHopCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                          CommandParameter="{Binding}"
                                                          ToolTip="Remove"
                                                          Width="28" Height="28"
                                                          Padding="0" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Empty State -->
                        <TextBlock Grid.Row="0"
                                  Text="No jump hosts added yet. Add hosts below to create a chain."
                                  Opacity="0.5"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Visibility="{Binding JumpHops.Count, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=Invert}" />

                        <!-- Add Host Section -->
                        <Grid Grid.Row="1" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ComboBox Grid.Column="0"
                                     ItemsSource="{Binding AvailableHosts}"
                                     SelectedItem="{Binding SelectedHostToAdd}"
                                     DisplayMemberPath="DisplayName"
                                     Margin="0,0,8,0">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}" />
                                            <TextBlock Text=" - " Opacity="0.5" />
                                            <TextBlock Text="{Binding Hostname}" Opacity="0.6" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <ui:Button Grid.Column="1"
                                      Icon="{ui:SymbolIcon Add16}"
                                      Content="Add"
                                      Command="{Binding AddJumpHopCommand}"
                                      Appearance="Primary"
                                      Padding="12,6" />
                        </Grid>
                    </Grid>
                </Border>

                <!-- Chain Preview -->
                <Border Background="#0C3A5E"
                        CornerRadius="6"
                        Padding="12"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Connection Path Preview"
                                  FontWeight="SemiBold"
                                  FontSize="11"
                                  Opacity="0.7"
                                  Margin="0,0,0,4" />
                        <TextBlock Text="{Binding ChainPreview}"
                                  TextWrapping="Wrap"
                                  Foreground="#80CAFF" />
                    </StackPanel>
                </Border>

                <!-- Validation Error -->
                <Border Background="#3A1A1A"
                        CornerRadius="6"
                        Padding="12"
                        Margin="0,0,0,8"
                        Visibility="{Binding ValidationError, Converter={StaticResource StringToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Warning24"
                                      Foreground="#FF6B6B"
                                      Margin="0,0,8,0" />
                        <TextBlock Text="{Binding ValidationError}"
                                  Foreground="#FF6B6B"
                                  TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Border Grid.Row="1"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="40" Height="40" />
                <TextBlock Text="Saving..." Margin="0,8,0,0" />
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="2" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button Grid.Column="1"
                      Content="Cancel"
                      Command="{Binding CancelCommand}"
                      Margin="0,0,8,0"
                      MinWidth="80" />

            <ui:Button Grid.Column="2"
                      Content="Save"
                      Appearance="Primary"
                      Command="{Binding SaveCommand}"
                      MinWidth="80" />
        </Grid>
    </Grid>
</ui:FluentWindow>
