<ui:FluentWindow x:Class="SshManager.App.Views.Windows.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                xmlns:terminal="clr-namespace:SshManager.Terminal.Controls;assembly=SshManager.Terminal"
                xmlns:converters="clr-namespace:SshManager.App.Converters"
                xmlns:controls="clr-namespace:SshManager.App.Views.Controls"
                xmlns:terminalServices="clr-namespace:SshManager.Terminal.Services;assembly=SshManager.Terminal"
                xmlns:behaviors="clr-namespace:SshManager.App.Behaviors"
                mc:Ignorable="d"
                Title="SSH Manager"
                Icon="pack://application:,,,/Resources/app-icon.ico"
                Height="700" Width="1100"
                MinHeight="500" MinWidth="800"
                WindowStartupLocation="CenterScreen"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        <converters:NullToPrimaryAppearanceConverter x:Key="NullToPrimaryAppearanceConverter" />
        <converters:HostStatusToColorConverter x:Key="HostStatusToColorConverter" />
        <converters:HostStatusToTooltipConverter x:Key="HostStatusToTooltipConverter" />
        <converters:HostActiveSessionBorderConverter x:Key="HostActiveSessionBorderConverter" />
        <converters:GroupColorConverter x:Key="GroupColorConverter" />
        <converters:GroupColorToVisibilityConverter x:Key="GroupColorToVisibilityConverter" />
    </ui:FluentWindow.Resources>

    <!-- Keyboard Shortcuts -->
    <ui:FluentWindow.InputBindings>
        <!-- Host Management -->
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding AddHostCommand}" />
        <KeyBinding Key="E" Modifiers="Ctrl" Command="{Binding EditHostCommand}" CommandParameter="{Binding SelectedHost}" />
        <KeyBinding Key="Delete" Command="{Binding DeleteHostCommand}" CommandParameter="{Binding SelectedHost}" />
        <KeyBinding Key="Return" Command="{Binding ConnectCommand}" CommandParameter="{Binding SelectedHost}" />

        <!-- Terminal Sessions -->
        <KeyBinding Key="W" Modifiers="Ctrl" Command="{Binding CloseSessionCommand}" CommandParameter="{Binding CurrentSession}" />
        <KeyBinding Key="F4" Modifiers="Ctrl" Command="{Binding CloseSessionCommand}" CommandParameter="{Binding CurrentSession}" />

        <!-- Navigation -->
        <KeyBinding Key="F" Modifiers="Ctrl" x:Name="SearchShortcut" />
        <KeyBinding Key="OemComma" Modifiers="Ctrl" x:Name="SettingsShortcut" />
        <KeyBinding Key="H" Modifiers="Ctrl" x:Name="HistoryShortcut" />
        <KeyBinding Key="S" Modifiers="Ctrl+Shift" x:Name="SnippetsShortcut" />
        <KeyBinding Key="K" Modifiers="Ctrl" x:Name="KeyManagerShortcut" />

        <!-- SFTP Browser -->
        <KeyBinding Key="B" Modifiers="Ctrl" Command="{Binding OpenSftpBrowserCommand}" />

        <!-- Refresh -->
        <KeyBinding Key="F5" Command="{Binding SearchCommand}" />
    </ui:FluentWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="SSH Manager"
                     ShowMaximize="True"
                     ShowMinimize="True" />

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="260" MaxWidth="400" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="300" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Host List -->
            <Grid Grid.Column="0" Background="{ui:ThemeResource ApplicationBackgroundBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Search Box -->
                <ui:TextBox x:Name="SearchBox"
                           Grid.Row="0"
                           PlaceholderText="Search hosts... (Ctrl+F)"
                           Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                           Margin="8"
                           Icon="{ui:SymbolIcon Search24}" />

                <!-- Groups Section - Compact Filter Bar -->
                <Grid Grid.Row="1" Margin="8,4,8,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Group Filter Dropdown -->
                    <ComboBox x:Name="GroupFilterCombo"
                             Grid.Column="0"
                             SelectedIndex="0"
                             SelectionChanged="GroupFilterCombo_SelectionChanged"
                             VerticalAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <!-- Color indicator circle (for groups with colors) -->
                                    <Ellipse Width="10" Height="10"
                                            Margin="0,0,6,0"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding Color, Converter={StaticResource GroupColorToVisibilityConverter}}">
                                        <Ellipse.Fill>
                                            <Binding Path="Color" Converter="{StaticResource GroupColorConverter}" />
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <ui:SymbolIcon Symbol="{Binding Icon}" Margin="0,0,8,0" FontSize="14" />
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Count, StringFormat=' ({0})'}"
                                              Opacity="0.6"
                                              VerticalAlignment="Center"
                                              Visibility="{Binding HasCount, Converter={StaticResource BoolToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- Add Group Button -->
                    <ui:Button Grid.Column="1"
                              Icon="{ui:SymbolIcon Add16}"
                              Command="{Binding AddGroupCommand}"
                              ToolTip="Add New Group"
                              Padding="8,6"
                              Margin="6,0,0,0" />
                </Grid>

                <!-- Host List -->
                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Hosts}"
                         SelectedItem="{Binding SelectedHost}"
                         Margin="8,8,8,0"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.VerticalScrollBarVisibility="Hidden"
                         ScrollViewer.CanContentScroll="False"
                         VirtualizingPanel.ScrollUnit="Pixel"
                         behaviors:ListBoxDragDropBehavior.IsEnabled="True"
                         behaviors:ListBoxDragDropBehavior.ReorderCommand="{Binding ReorderHostsCommand}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,8" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Outer border for active session indicator -->
                            <Border CornerRadius="8"
                                   BorderThickness="2"
                                   Padding="0">
                                <Border.BorderBrush>
                                    <MultiBinding Converter="{StaticResource HostActiveSessionBorderConverter}">
                                        <Binding Path="Id" />
                                        <Binding Path="DataContext.Sessions" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                    </MultiBinding>
                                </Border.BorderBrush>
                            <Grid>
                                <!-- Colored left stripe for group color -->
                                <Border Width="4"
                                       HorizontalAlignment="Left"
                                       CornerRadius="6,0,0,6"
                                       Visibility="{Binding Group.Color, Converter={StaticResource GroupColorToVisibilityConverter}}">
                                    <Border.Background>
                                        <Binding Path="Group.Color" Converter="{StaticResource GroupColorConverter}" />
                                    </Border.Background>
                                </Border>
                            <Border x:Name="CardBorder"
                                   CornerRadius="6"
                                   Background="#1FFFFFFF"
                                   BorderBrush="#2FFFFFFF"
                                   BorderThickness="1"
                                   Padding="8,6"
                                   RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform ScaleX="1" ScaleY="1" />
                                        <TranslateTransform X="0" Y="0" />
                                    </TransformGroup>
                                </Border.RenderTransform>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                        From="0" To="1"
                                                                        Duration="0:0:0.3">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CubicEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)"
                                                                        From="20" To="0"
                                                                        Duration="0:0:0.3">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CubicEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                            <EventTrigger RoutedEvent="MouseEnter">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                                                        To="1.02" Duration="0:0:0.15">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                                                        To="1.02" Duration="0:0:0.15">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                       To="#2AFFFFFF" Duration="0:0:0.15" />
                                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                                       To="#3FFFFFFF" Duration="0:0:0.15" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                            <EventTrigger RoutedEvent="MouseLeave">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                                                        To="1" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                                                        To="1" Duration="0:0:0.2">
                                                            <DoubleAnimation.EasingFunction>
                                                                <QuadraticEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>
                                                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                       To="#1FFFFFFF" Duration="0:0:0.2" />
                                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                                       To="#2FFFFFFF" Duration="0:0:0.2" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Status Indicator Dot -->
                                    <Ellipse Grid.Row="0" Grid.RowSpan="2"
                                            Grid.Column="0"
                                            Width="8" Height="8"
                                            VerticalAlignment="Center"
                                            Margin="0,0,8,0">
                                        <Ellipse.Fill>
                                            <MultiBinding Converter="{StaticResource HostStatusToColorConverter}">
                                                <Binding Path="Id" />
                                                <Binding Path="DataContext.HostStatuses" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                            </MultiBinding>
                                        </Ellipse.Fill>
                                        <Ellipse.ToolTip>
                                            <MultiBinding Converter="{StaticResource HostStatusToTooltipConverter}">
                                                <Binding Path="Id" />
                                                <Binding Path="DataContext.HostStatuses" RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                            </MultiBinding>
                                        </Ellipse.ToolTip>
                                    </Ellipse>

                                    <!-- Row 1: Name and Action Buttons -->
                                    <TextBlock Grid.Row="0" Grid.Column="1"
                                              Text="{Binding DisplayName}"
                                              FontWeight="SemiBold"
                                              FontSize="13"
                                              VerticalAlignment="Center"
                                              TextTrimming="CharacterEllipsis" />

                                    <StackPanel Grid.Row="0" Grid.Column="2"
                                               Orientation="Horizontal"
                                               VerticalAlignment="Center"
                                               Margin="4,0,0,0">
                                        <ui:Button Width="24" Height="24"
                                                  Padding="0"
                                                  CornerRadius="4"
                                                  Command="{Binding DataContext.OpenSftpBrowserForHostCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                  CommandParameter="{Binding}"
                                                  ToolTip="SFTP Browser"
                                                  Margin="0,0,2,0">
                                            <ui:Button.Style>
                                                <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                                    <Setter Property="Background" Value="#0078D4" />
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#1084D8" />
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:Button.Style>
                                            <ui:SymbolIcon Symbol="FolderOpen16" FontSize="12" Foreground="White" />
                                        </ui:Button>
                                        <ui:Button Icon="{ui:SymbolIcon Edit16}"
                                                  Command="{Binding DataContext.EditHostCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                  CommandParameter="{Binding}"
                                                  ToolTip="Edit (Ctrl+E)"
                                                  Width="24" Height="24"
                                                  Padding="0"
                                                  Margin="0,0,2,0" />
                                        <ui:Button Icon="{ui:SymbolIcon Play16}"
                                                  Appearance="Primary"
                                                  Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                  CommandParameter="{Binding}"
                                                  ToolTip="Connect (Enter)"
                                                  Width="24" Height="24"
                                                  Padding="0" />
                                    </StackPanel>

                                    <!-- Row 2: Group and Hostname -->
                                    <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                               Orientation="Horizontal"
                                               Margin="0,2,0,0">
                                        <!-- Small color dot for group color -->
                                        <Ellipse Width="6" Height="6"
                                                Margin="0,0,4,0"
                                                VerticalAlignment="Center"
                                                Visibility="{Binding Group.Color, Converter={StaticResource GroupColorToVisibilityConverter}}">
                                            <Ellipse.Fill>
                                                <Binding Path="Group.Color" Converter="{StaticResource GroupColorConverter}" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock Text="{Binding Group.Name, FallbackValue='Ungrouped'}"
                                                  Opacity="0.5"
                                                  FontSize="11"
                                                  FontStyle="Italic"
                                                  TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text=" · "
                                                  Opacity="0.4"
                                                  FontSize="11" />
                                        <TextBlock Text="{Binding Hostname}"
                                                  Opacity="0.7"
                                                  FontSize="11"
                                                  TextTrimming="CharacterEllipsis"
                                                  MaxWidth="160" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                            </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Action Buttons -->
                <Grid Grid.Row="3" Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Left: Settings, History, Snippets, and Key Manager buttons -->
                    <StackPanel Grid.Column="0"
                               Orientation="Horizontal">
                        <ui:Button Icon="{ui:SymbolIcon Settings24}"
                                  ToolTip="Settings (Ctrl+,)"
                                  Click="SettingsButton_Click"
                                  Margin="4" />
                        <ui:Button Icon="{ui:SymbolIcon History24}"
                                  ToolTip="Connection History (Ctrl+H)"
                                  Click="HistoryButton_Click"
                                  Margin="4" />
                        <ui:Button Icon="{ui:SymbolIcon Code24}"
                                  ToolTip="Snippets (Ctrl+Shift+S)"
                                  Click="SnippetsButton_Click"
                                  Margin="4" />
                        <ui:Button Icon="{ui:SymbolIcon Key24}"
                                  ToolTip="SSH Key Manager (Ctrl+K)"
                                  Click="KeyManagerButton_Click"
                                  Margin="4" />
                    </StackPanel>

                    <!-- Center: Add Host button -->
                    <ui:Button Grid.Column="1"
                               Icon="{ui:SymbolIcon Add24}"
                               Appearance="Primary"
                               Command="{Binding AddHostCommand}"
                               ToolTip="Add Host (Ctrl+N)"
                               HorizontalAlignment="Center"
                               Margin="4" />

                </Grid>
            </Grid>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                         Width="4"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Stretch"
                         Background="Transparent" />

            <!-- Right Panel: Terminal Area -->
            <Grid Grid.Column="2">
                <!-- Session Tabs and Terminal - using separate tab strip and content for reliable switching -->
                <Grid Visibility="{Binding HasActiveSessions, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Tab Strip with SFTP Toggle -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Session Tabs -->
                        <ListBox x:Name="SessionTabs"
                                Grid.Column="0"
                                ItemsSource="{Binding Sessions}"
                                SelectedItem="{Binding CurrentSession, Mode=TwoWay}"
                                SelectionChanged="SessionTabs_SelectionChanged"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="0,4,4,0"
                                Margin="8,0,0,0">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,0,2,0" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10,10,0,0"
                                           Padding="12,8">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{ui:ThemeResource ControlFillColorDefaultBrush}" />
                                                <Setter Property="BorderBrush" Value="Transparent" />
                                                <Setter Property="BorderThickness" Value="2" />
                                                <Style.Triggers>
                                                    <!-- Blue border when tab is selected (active) -->
                                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                        <Setter Property="BorderBrush" Value="#0078D4" />
                                                    </DataTrigger>
                                                    <!-- Highlight when selected for broadcast -->
                                                    <DataTrigger Binding="{Binding IsSelectedForBroadcast}" Value="True">
                                                        <Setter Property="Background" Value="#40FF5500" />
                                                        <Setter Property="BorderBrush" Value="#FF5500" />
                                                        <Setter Property="BorderThickness" Value="2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <!-- Broadcast selection checkbox (visible when broadcast mode is on) -->
                                            <CheckBox IsChecked="{Binding IsSelectedForBroadcast, Mode=TwoWay}"
                                                     Visibility="{Binding DataContext.IsBroadcastMode, RelativeSource={RelativeSource AncestorType=ListBox}, Converter={StaticResource BoolToVisibilityConverter}}"
                                                     VerticalAlignment="Center"
                                                     Margin="0,0,6,0"
                                                     ToolTip="Include in broadcast" />
                                            <!-- Group color indicator -->
                                            <Ellipse Width="8" Height="8"
                                                    Margin="0,0,6,0"
                                                    VerticalAlignment="Center"
                                                    Visibility="{Binding Host.Group.Color, Converter={StaticResource GroupColorToVisibilityConverter}}">
                                                <Ellipse.Fill>
                                                    <Binding Path="Host.Group.Color" Converter="{StaticResource GroupColorConverter}" />
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <TextBlock Text="{Binding Title}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                            <ui:Button Icon="{ui:SymbolIcon Dismiss12}"
                                                      Padding="2"
                                                      Background="Transparent"
                                                      Command="{Binding DataContext.CloseSessionCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                      CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Terminal Toolbar Buttons -->
                        <StackPanel Grid.Column="1"
                                   Orientation="Horizontal"
                                   VerticalAlignment="Center"
                                   Margin="4,4,8,4">
                            <!-- Broadcast Mode Toggle -->
                            <Grid Margin="0,0,8,0">
                                <ui:Button Command="{Binding ToggleBroadcastModeCommand}"
                                          Padding="8">
                                    <ui:Button.Style>
                                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                            <Setter Property="ToolTip" Value="Enable Broadcast Mode (type in all sessions)" />
                                            <Setter Property="Icon" Value="{ui:SymbolIcon Send24}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsBroadcastMode}" Value="True">
                                                    <Setter Property="ToolTip">
                                                        <Setter.Value>
                                                            <StackPanel>
                                                                <TextBlock Text="Broadcast Mode Active" FontWeight="SemiBold" />
                                                                <TextBlock Text="{Binding BroadcastSelectedCount, StringFormat='Typing to {0} session(s)'}" Opacity="0.7" />
                                                            </StackPanel>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="Background" Value="#FF5500" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:Button.Style>
                                </ui:Button>
                                <!-- Broadcast Indicator (Orange dot) -->
                                <Ellipse Width="8" Height="8"
                                        Fill="#FF5500"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,2,2,0"
                                        IsHitTestVisible="False"
                                        Visibility="{Binding IsBroadcastMode, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                            <!-- Broadcast Select All / Deselect All (visible when broadcast mode is on) -->
                            <StackPanel Orientation="Horizontal"
                                       Visibility="{Binding IsBroadcastMode, Converter={StaticResource BoolToVisibilityConverter}}"
                                       Margin="0,0,8,0">
                                <ui:Button Icon="{ui:SymbolIcon SelectAllOn24}"
                                          ToolTip="Select all sessions for broadcast"
                                          Command="{Binding SelectAllForBroadcastCommand}"
                                          Padding="6"
                                          Margin="0,0,2,0" />
                                <ui:Button Icon="{ui:SymbolIcon SelectAllOff24}"
                                          ToolTip="Deselect all sessions"
                                          Command="{Binding DeselectAllForBroadcastCommand}"
                                          Padding="6" />
                            </StackPanel>
                            <!-- Separator -->
                            <Border Width="1" Background="#3FFFFFFF" Margin="4,4,8,4" />
                            <!-- Session Recording Toggle -->
                            <Grid>
                                <ui:Button Command="{Binding ToggleSessionLoggingCommand}"
                                          Padding="8"
                                          Margin="0,0,4,0">
                                    <ui:Button.Style>
                                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                            <Setter Property="ToolTip" Value="Start session logging" />
                                            <Setter Property="Icon" Value="{ui:SymbolIcon Record24}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCurrentSessionLogging}" Value="True">
                                                    <Setter Property="ToolTip">
                                                        <Setter.Value>
                                                            <StackPanel>
                                                                <TextBlock Text="Recording - Click to stop" FontWeight="SemiBold" />
                                                                <TextBlock Text="{Binding CurrentSessionLogPath}" Opacity="0.7" />
                                                            </StackPanel>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="Icon" Value="{ui:SymbolIcon RecordStop24}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:Button.Style>
                                </ui:Button>
                                <!-- Recording Indicator (Red dot) -->
                                <Ellipse Width="8" Height="8"
                                        Fill="#E81123"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,2,2,0"
                                        IsHitTestVisible="False"
                                        Visibility="{Binding IsCurrentSessionLogging, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                            <!-- Session Log Options -->
                            <ui:DropDownButton Icon="{ui:SymbolIcon Settings24}"
                                              ToolTip="Session log options"
                                              Padding="8"
                                              Margin="0,0,4,0">
                                <ui:DropDownButton.Flyout>
                                    <ContextMenu>
                                        <ui:MenuItem Header="Output + Events"
                                                     Command="{Binding SetCurrentSessionLogLevelCommand}"
                                                     CommandParameter="{x:Static terminalServices:SessionLogLevel.OutputAndEvents}" />
                                        <ui:MenuItem Header="Events Only"
                                                     Command="{Binding SetCurrentSessionLogLevelCommand}"
                                                     CommandParameter="{x:Static terminalServices:SessionLogLevel.EventsOnly}" />
                                        <ui:MenuItem Header="Errors Only"
                                                     Command="{Binding SetCurrentSessionLogLevelCommand}"
                                                     CommandParameter="{x:Static terminalServices:SessionLogLevel.ErrorsOnly}" />
                                        <Separator />
                                        <ui:MenuItem Header="Redact typed secrets"
                                                     IsCheckable="True"
                                                     IsChecked="{Binding CurrentSessionRedactTypedSecrets}" />
                                    </ContextMenu>
                                </ui:DropDownButton.Flyout>
                            </ui:DropDownButton>
                            <!-- Open Current Log -->
                            <ui:Button Icon="{ui:SymbolIcon Open24}"
                                      ToolTip="Open current session log file"
                                      Command="{Binding OpenCurrentSessionLogFileCommand}"
                                      Padding="8"
                                      Margin="0,0,4,0" />
                            <!-- Open Log Directory -->
                            <ui:Button Icon="{ui:SymbolIcon FolderOpen24}"
                                      ToolTip="Open log directory"
                                      Command="{Binding OpenLogDirectoryCommand}"
                                      Padding="8"
                                      Margin="0,0,4,0" />
                            <!-- SFTP Browser Button -->
                            <ui:Button Icon="{ui:SymbolIcon Document24}"
                                      ToolTip="Open SFTP File Browser (Ctrl+B)"
                                      Command="{Binding OpenSftpBrowserCommand}"
                                      Padding="8"
                                      Margin="0,0,4,0" />
                            <!-- Port Forwarding Button with Flyout -->
                            <Grid>
                                <ui:DropDownButton x:Name="PortForwardingButton"
                                                  Icon="{ui:SymbolIcon ArrowRouting24}"
                                                  Padding="8">
                                    <ui:DropDownButton.ToolTip>
                                        <StackPanel>
                                            <TextBlock Text="Port Forwarding" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding PortForwardingManager.ActiveSummary}" Opacity="0.7" />
                                        </StackPanel>
                                    </ui:DropDownButton.ToolTip>
                                    <ui:DropDownButton.Flyout>
                                        <ContextMenu MinWidth="320">
                                            <ContextMenu.Template>
                                                <ControlTemplate>
                                                    <Border Background="{DynamicResource MenuFlyoutPresenterBackground}"
                                                           BorderBrush="{DynamicResource MenuFlyoutPresenterBorderBrush}"
                                                           BorderThickness="1"
                                                           CornerRadius="8"
                                                           Padding="8">
                                                        <controls:PortForwardingStatusPanel
                                                            DataContext="{Binding DataContext.PortForwardingManager, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                            ManageProfilesRequested="PortForwardingPanel_ManageProfilesRequested" />
                                                    </Border>
                                                </ControlTemplate>
                                            </ContextMenu.Template>
                                        </ContextMenu>
                                    </ui:DropDownButton.Flyout>
                                </ui:DropDownButton>
                                <!-- Active Indicator (Green dot when forwards active) -->
                                <Ellipse Width="8" Height="8"
                                        Fill="#4CAF50"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,2,2,0"
                                        IsHitTestVisible="False"
                                        Visibility="{Binding PortForwardingManager.HasActiveForwardings, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Terminal Pane Container - supports split panes -->
                    <Border Grid.Row="1"
                            Background="#0C0C0C"
                            CornerRadius="4"
                            Margin="4,0,4,4"
                            ClipToBounds="True">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="#2A2A2A" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Style.Triggers>
                                    <!-- Orange border when broadcast mode is active -->
                                    <DataTrigger Binding="{Binding IsBroadcastMode}" Value="True">
                                        <Setter Property="BorderBrush" Value="#FF5500" />
                                        <Setter Property="BorderThickness" Value="2" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <controls:TerminalPaneContainer x:Name="PaneContainer"
                                                        PaneSplitRequested="PaneContainer_PaneSplitRequested"
                                                        PaneCloseRequested="PaneContainer_PaneCloseRequested"
                                                        SessionDisconnected="PaneContainer_SessionDisconnected" />
                    </Border>
                </Grid>

                <!-- Welcome Message (when no sessions) -->
                <StackPanel VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Visibility="{Binding HasActiveSessions, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <Border Background="#1FFFFFFF"
                           CornerRadius="50"
                           Width="100" Height="100"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,24">
                        <ui:SymbolIcon Symbol="DesktopSignal24"
                                      FontSize="48"
                                      Opacity="0.7"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="Welcome to SSH Manager"
                              FontSize="28"
                              FontWeight="SemiBold"
                              HorizontalAlignment="Center"
                              Margin="0,0,0,8" />
                    <TextBlock Text="Select a host from the list and click Connect to start a session"
                              Opacity="0.6"
                              FontSize="14"
                              HorizontalAlignment="Center"
                              Margin="0,0,0,24" />
                    <StackPanel Orientation="Horizontal"
                               HorizontalAlignment="Center">
                        <ui:Button Icon="{ui:SymbolIcon Add24}"
                                  Content="Add Your First Host"
                                  Appearance="Primary"
                                  Command="{Binding AddHostCommand}"
                                  Padding="16,10"
                                  FontSize="14"
                                  Margin="0,0,12,0" />
                        <ui:DropDownButton Icon="{ui:SymbolIcon ArrowDownload24}"
                                          Content="Import Hosts"
                                          Padding="16,10"
                                          FontSize="14">
                            <ui:DropDownButton.Flyout>
                                <ContextMenu>
                                    <ui:MenuItem Header="From File..."
                                                Icon="{ui:SymbolIcon Document24}"
                                                Click="ImportHostsButton_Click" />
                                    <ui:MenuItem Header="From SSH Config..."
                                                Icon="{ui:SymbolIcon Code24}"
                                                Click="ImportFromSshConfigButton_Click" />
                                    <ui:MenuItem Header="From PuTTY..."
                                                Icon="{ui:SymbolIcon WindowConsole20}"
                                                Click="ImportFromPuttyButton_Click" />
                                </ContextMenu>
                            </ui:DropDownButton.Flyout>
                        </ui:DropDownButton>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
