<ui:FluentWindow x:Class="SshManager.App.Views.Windows.SftpBrowserWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                xmlns:controls="clr-namespace:SshManager.App.Views.Controls"
                mc:Ignorable="d"
                Title="{Binding WindowTitle}"
                Width="1000" Height="700"
                MinWidth="800" MinHeight="500"
                WindowStartupLocation="CenterOwner"
                ExtendsContentIntoTitleBar="True"
                WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="{Binding WindowTitle}"/>

        <!-- SFTP Browser Control (reuse existing) -->
        <controls:SftpBrowserControl Grid.Row="1"
                                      DataContext="{Binding SftpBrowser}"/>
    </Grid>
</ui:FluentWindow>
